# 2026-09-16 · 出刊文案正式化与英文版报头字体设计

## 一、占位文案清理（用户要求「正式上线后应替换优化」）

**广告位口径改写**：`i18n.mjs` 的 `adHead` / `adBody` / `adSign` 三键中英文全部重写。
- 原文案是「订阅启事 / Subscription Notice」，描述的是**本站并不存在的订阅业务**
  （全年订阅价洋十元、清晨六时出版、向各地邮局接洽）——样板站残留的虚构文案。
- 改为「广告招租 / Advertising Space to Let」：显著版位招租、价目从优、长期优待、洽广告部。
  仍走民国报纸广告腔（商号启事、新张志庆、货品推销、招雇声明），但不再虚构订阅关系。

**创刊年份动态化**：`founded` 原硬编码「创刊于二〇二五年」——2026 年已是假信息。
- `dateCn.mjs` 新增 `cnYear()`；`i18n.mjs` 的 `founded` 改为 `y => ...` 函数；
- `FrontPage.astro` / `Archive.astro` 各自从**最早期次**推得年份（`listIssueDates().at(-1)`），
  经 `Masthead` 的 `foundedYear` prop 传入。跨年不再需要改文案。

**清理死键**：`siteName` / `archiveMeta` / `langSwitchLabel` / `colophonNav` 四键全站零引用（已 grep 核实），删除。

**附带修复**：英文版页面的 `<meta name="description">` 一直落到 `Page.astro` 的中文默认值
（6 个页面都没传 `description`）。现各页面显式传 `T.description`，英文页描述已随语言。

## 二、双语版差异的显著标识（新增）

**背景**：zh / en 两版由 `buildEdition` **各自独立选稿**，头条、栏目、简讯、副刊均不同源
（实测当日 zh 头条「外交部…高市早苗涉台」，en 头条「Nato jets down drone…」），
但页面上**没有任何说明**，读者极易误以为二者是同一份报纸的两个语言版本而互相参校。

**实现**：新增 `src/components/EditionNotice.astro`，置于**报头之下、栏目条之上**（报纸「启事」的惯常位置）。
- 三段结构：套红标签「读者须知 / A Note to Readers」+**套红加粗关键句**
  （「中、英两版内容并不相同，亦非逐篇互译」）+ 说明正文与切换链接。
- 样式 §3c：纸暗底 + 上下细线夹持，正文 `max-width: 62ch` 限宽。
- 挂载点：`FrontPage.astro` 与 `Archive.astro` —— 覆盖全部 6 个入口页。
- 打印态隐藏切换链接（纸上点不动）。

## 三、英文版报头：刊名层级镜像反转 + 字体设计

**用户要求**：英文版「大标题是英文、小标题才是中文，与中文版恰恰相反」，且需为字体做合理设计。

**实现**（`Masthead.astro` + CSS §3b）——两版互为镜像：

| 位置 | zh 版 | en 版 |
|---|---|---|
| **大字位**（h1） | 每日新報 · Noto Serif SC 900 · 88px · 字距 .22em · 双钩描边 | The Daily News · Playfair Display **900** · **72px** · 字距 **.02em** · **无描边** |
| **小字位** | The Daily News · IM Fell 斜体套红 | 每日新報 · Noto Serif SC 700 套红 · 字距 .3em |

**为何不能沿用中文刊名那套处理**（三项各有物理原因，已写入 CSS 注释）：
1. `.22em` 字距——汉字等宽，拉开是端庄；拉丁小写比例字宽参差，拉开成「字间漏风」；
2. 双钩描边——为补宋体细笔画墨量而设；Playfair 横竖对比本强，再描边衬线会糊成一团；
3. 88px——「The Daily News」14 字符横向占位约为「每日新報」4 字的 1.6 倍，沿用会挤掉两侧期数/日期栏。

**新增 Playfair Display 900 字重**：中文刊名是宋体 900，拉丁若停在 700 同一行会明显「轻一头」。
latin 分片 22KB（与 700 同量级），代价极小。`fonts.css` 加 import、`vendor-fonts.sh` 字重清单同步。

**顺带修正 `vendor-fonts.sh` 输出路径**：`OUT` 原为 `$ROOT/fonts`（项目根），与实际使用目录
`public/fonts`（`css/fonts.css` 以 `../fonts/` 引用）不符——跑脚本会把产物丢进没人读的目录。

## 四、验证（全部实测，非推断）

- 单测 **95/95 通过**；`astro build` 6 页通过。
- `verify:layout --mode=ship`：**硬失败 0**（clipped / hOver / docOver / cFail 全 0），
  告警 156 与改动前持平（未新增）。
- 8 档视口实测英文报头（375 / 600 / 768 / 900 / 1024 / 1100 / 1280 / 1440）：

  | 档位 | 字号 | 刊名墨迹宽 | 两侧余量 | 溢出 |
  |---|---|---|---|---|
  | ≥1100 | 72px | 541px | 各 264px | 无 |
  | 900–1099 | 52px | 391px | 191–291px | 无 |
  | ≤768 | 44px | 占满（单列堆叠） | — | 无 |

  全部单行、无内部溢出；`刊名墨迹→日期文字` 间距与中文版**同为 16px**（用 Range 量墨迹，非盒子宽）。

- 截图证据：`docs/typography-optimization/screenshots/i18n-{zh,en}-{masthead,advert}-1280.png`
  与 `i18n-{zh,en}-masthead-375.png`。

## 五、已知观察（未改，留待决策）

- **英文版栏目标/标签字距偏大**：`.sections li`(.45em)、`.briefs-title`(.5em)、
  `.section-label`/`.supplement-kicker`(.4em)、`.translated-title`(.4em)、`.archive-title`(.5em)。
  这些 .4–.5em 是为中文方块的「庄重感」所设，套在英文词上会散成「W o r l d」。
  本次只处理了直接触及的三处（报头、广告标题、声明带标签）；其余属既有问题，
  且改动会牵动栏目条宽度平衡（`.sections li` padding 2rem），建议单独评估。
- **英文版栏目块留白 52px**：`section-block` 同排等高（grid stretch），内容少的一侧留白。
  与本次改动无关（栏目内容未变），ship 模式判为告警非失败。
- **375px 下英文农历行偏满**：「5th day of the 8th lunar month · White Dew」几乎占满版心
  （未溢出，`docOver=false`）。属数据文本长度问题，非版式缺陷。

---

## 六、第二轮：声明带可关闭 + 去售价 + 期号从第一期起（同日 11:00–11:10）

### 6.1 读者须知可关闭（用户：允许关闭，5 秒倒计时自己关闭）

**约束**：本站对外承诺「零脚本静态页面」（colophon 明写），不为一个关闭按钮破例。

**实现（全程零 JS，`EditionNotice.astro` + CSS §3c）**：
- **手动**：右上角 × 是个 `<label for>`，切换一个视觉隐藏但可聚焦的 `<input type="checkbox">`，
  由 `.edition-notice-off:checked ~ .edition-notice-clip { display: none }` 收起；
  `:focus-visible` 时给 × 加虚线焦点环（键盘可达）。
- **自动**：5 秒后 CSS 动画把外层 `.edition-notice-slot` 的 `grid-template-rows` 由 `1fr` 收到 `0fr`，
  内层 `overflow: hidden` 完成塌陷。
  **为什么走 grid 行高**：`height: auto → 0` 不可插值；`display` 的离散动画旧浏览器不可靠；
  grid 行高是唯一「既能插值、又能把高度真正收掉」的免脚本路径。
- **倒计时可见化**：底边 2px 红线是进度条（`width: 100% → 0`，与外层同 5s／同延迟），
  让「会自己消失」可预期——否则内容凭空消失会像 bug。

**实测**（agent-browser 逐时刻量测）：

| 时刻 | slot 高 | 进度条宽 |
|---|---|---|
| T+0.3s | 145px | 851px |
| T+3.3s | 145px | 106px |
| T+6.9s | **0** | 0 |
| 手动点 × 后 | **0**（clip 为 `display: none`） | 0 |

**已知取舍**：收起后本次访问内无法再展开（CSS 动画的 forwards 状态改不回来，纯 CSS 也无「记住已读」）。
要「可重开／跨次记住」就必须引入 JS，届时 colophon 的「零脚本」表述须同步改。
另需注意：**这条声明是内容诚信的一部分**，自动消失有代价（读者可能没读完），进度条是对可知性的缓解。

无障碍：属 WCAG 2.2.2「自动消失内容」，已提供关闭钮与可见倒计时条；两者为指示性而非运动性动画，
不触发前庭不适，故不另设 `prefers-reduced-motion` 特例（理由已写进 CSS 注释）。
打印态停用动画并隐藏关闭钮／进度条。

### 6.2 删除「零售价 贰角」

`Masthead.astro` 删 `<p class="issue-price">`，`i18n.mjs` 删 `price` 键（zh/en）。
六页产物复查「售价残留：无」。报头左侧现只剩期号一行；`align-items: end` 使期号与右侧底部
（语言切换）齐平，符合报头惯例。

### 6.3 期号改为「从第一期起算」

**旧口径**：`ISSUE_EPOCH = Date.UTC(2025, 6, 14)`（虚构创刊日），`(date - epoch)/864e5 + 1`——
最初是为了让 2026-09-14 恰好等于第 428 期以对齐 M1 原型版面。
它算的是**日历天数差**而非出刊期数：缺期会跳号，且与站上实际期数无关（站上只有一期却显示 429）。

**新口径**（第一期 = 1，以磁盘上实际出刊序列为准）：
- 渲染层 `src/lib/issues.mjs` 新增 `issueNumberOf(date)` = `dates.length - indexOf(date)`；
- 生成层 `scripts/generate-issue.mjs` 的 `issueNo()` 改为扫 `issues/` 目录排序取位次，
  **两处必须同口径**（已在两边注释互相标明），否则报头与过刊页会不一致；
- 消费方改用派生值而非数据字段：`FrontPage.astro`（报头／印章／版尾）、
  `archive.astro` / `en/archive.astro`（列表行）；
- `issues/2026-09-15.json` 的 `issue` 字段同步 429 → 1，印章（`seal-issue`）随之显示「第〇〇〇一期 / No. 1」。

**验证**：6 页产物期号全部为「第〇〇〇一期」/「No. 1」，印章同步，售价无残留。

### 6.4 第二轮验证

- 单测 **95/95**；`astro build` 6 页；`verify:layout --mode=ship` **硬失败 0**、告警 156（与前一轮持平）。
- 截图：`i18n-en-notice-countdown-1280.png`（倒计时中，× 可见）、
  `i18n-en-notice-collapsed-1280.png`（收起后版面闭合、无空洞）。

---

## 七、仓库更新与提交前审计（同日 11:12–11:22）

### 7.1 审计发现的阻塞问题（已修复）

**问题**：推送被拒（`remote contains work that you do not have locally`）。fetch 后见远端多出
`bc6e242 chore: issue 2026-09-16` —— 今早 06:00 的 daily-issue 定时出刊。

**根因**：定时任务运行时，**远端 main 还没有双语管道**（M6 未推送，远端停在 M5 阶段），
于是旧代码生成了**旧单语言格式**的期数据：
- 顶层字段是 `issue,date,lunar,weather,headline,secondary,briefs,sections`，
  **没有 `languages` / `editions`**；期号是旧口径 430。
- 渲染层 `FrontPage.astro` 读 `issue.editions?.[lang]` → `undefined` →
  `ed?.headline` / `ed.briefs` / `ed.sections` 全空。
- **实测后果**：`dist/index.html` 仅 3148 字节，头条与栏目**全部为空**——
  而它是最新期，所以**首页就是一张空白报纸**。

**修复**：用当前管道重新出刊（`node scripts/generate-issue.mjs`，36/36 源、797 条素材），
覆盖为双语格式（`issue: 2`、`languages:[zh,en]`、`editions:{zh,en}`），并补上缺失的
`.meta.json` 与两张 OG 图。验证：zh 首页「第〇〇〇二期 / 国台办回应台海谍战剧《交锋》受关注」，
en 首页「No. 2 / Australian politics live…」，栏目齐全。

**教训**：**定时出刊只保证「数据」，不保证「数据与渲染层同代」**。
只要本地积着未推送的管道改动，远端 CI 就会用旧代码产出与当前渲染器不兼容的数据。
推送前应先 fetch 看远端是否已自行出刊；若已出刊且格式代际不符，须重新出刊覆盖。

### 7.2 GitHub Actions 政策影响（结论：不影响）

- 未改动 `.github/workflows/`，两工作流的触发条件与权限均未变。
- `daily-issue.yml`（UTC 22:00 / 北京 06:00）：install → **pnpm test** → build:issue → build →
  提交 `issues/` → push → `workflow_dispatch deploy.yml`。本次改动对其影响：
  ①测试须全绿（95 用例，已实测）；②`build:issue` 走新期号口径（实测 2026-09-16 得「第 2 期」✓）；
  ③未动 package.json / lockfile，`--frozen-lockfile` 不受影响。
- `deploy.yml`（push main / workflow_dispatch）：install → test → build → upload dist → Pages。
  **不跑 `verify:layout`**（需 chromium），故新增守门脚本不影响 CI。
- **`public/fonts/` 必须入库**：新增的 Playfair 900 分片若漏提交，CI 构建的英文刊名会退化为
  合成粗体。已确认 4 个 woff2 + `900.css` 均已提交。
- 推送 main 会**立即触发一次部署**（本次已触发并成功上线）。

### 7.3 README 更新（必要，多处已过时）

| 位置 | 原 | 新 |
|---|---|---|
| 头图 | `v2-zh-top.png`（旧版式：有售价、429 期、旧报头） | 当前版式截图 |
| 用例数 | 74 | 95 |
| 双语一节 | 「报头刊名两版共用」**（已错）** | 刊名随版本镜像排布，两套字体处理互不可换 |
| 双语一节 | — | 补声明带（可关闭·5 秒自动收起·零脚本）与期号口径 |
| 排版系统 | v3（`max-height` + 渐隐裁字）**（该机制已移除）** | v5 以宽度换高度、求解栏宽比例、不裁字 |
| 响应式断点 | 1020 / 680 | ≥1100 / 600–1099 / ≤599 |
| 结构 | 缺 verify-layout、vendor-fonts、public/fonts | 补齐；`issues.mjs` 注明期号派生 |

### 7.4 提交与推送

- `8f2409e feat: 排版 v5（以宽度换高度）+ 出刊文案正式化与英文版报头字体设计`（55 文件，1904+/741−）
- `9fe21ab fix: 用双语管道重新出刊 2026-09-16（远端定时期为旧格式，渲染为空）`
- 推送 `bc6e242..9fe21ab`，本地与远端同步；deploy-pages 触发并**已上线**——
  线上实测：期号「第〇〇〇二期」、头条「国台办回应…」、售价无残留、声明带在、
  `The Daily News` 大字在。

### 7.5 遗留注意事项

- **工作区仍有一个垃圾文件**：`docs/typography-optimization/screenshots/ .png`
  （文件名 = 一个空格 + `.png`，1.8MB，截图脚本参数错误的产物）。
  本次**未提交**（已从暂存区排除），仍留在工作区待清理。
- **仓库体积**：`docs/` 下截图约 74MB（含本次新增约 10MB）。GitHub 单仓库建议 <1GB，
  暂不致命；若要瘦身可压缩截图或把历史证据移出仓库。
- **明日起的 06:00 定时出刊走新管道**（新代码已在远端），不会再产出旧格式数据。

---

## 八、简讯补来源与原文链接 + 清理垃圾文件（同日 13:39–13:52）

### 8.1 简讯的 source / link 被整体丢弃（用户指出）

**问题**：`Briefs.astro` 只渲染 `title` / `lead` / `categoryLabel`，
数据里的 **`source` 与 `link` 从未被渲染**——读者既不知消息出处，也无从点进原文，
与头条、各版（都有「来源：…」行）不一致。当期 16 条简讯的 source/link **无一为空**，
属「数据齐备而渲染层漏掉」。

**实现**：新增 `.brief-src`，`来源：<媒体>` 之中**媒体名即原文链接**——
一个锚点同时承担「标注出处」与「可达原文」。
不照搬各版的「来源：X · 原文」两段式：简讯是密集列表（dense 双列每列仅 544px，
标题墨迹已可达 323px），多一个「· 原文」既冗赘又占宽，而「来源名可点」已是全站一致的链接语汇。
`.brief-src` 用 `inline` 跟随标题（`margin-left: .7em` + `--fs-fine` + 淡墨 + `nowrap`），
**不另起一行、不改变既有版式结构**。

**实测**：

| 视口 | 简讯栏高 | li 高度种类 | 来源溢出 |
|---|---|---|---|
| 1280（双列 544px） | 405 → **429px** | 41 / 65 | 0 |
| 768（双列 330px） | — | 65 | 0 |
| 375（单列 312px） | — | 64 / 65 / 88 | 0 |

高度仅 +24px（1 条因标题长而多一行）；三档 `overflowingSrc` 与 `docOx` 均为 0。
`verify:layout --mode=ship` **硬失败 0**；告警 156→162，逐项核对与本次改动无关——
`gap` 检测的选择器不含简讯，`estDev` 是 v5 后不再被消费的估算监视器，
且当期数据本身已换成 2026-09-16。

### 8.2 清理垃圾文件

删除 `docs/typography-optimization/screenshots/ .png`（文件名 = 一个空格 + `.png`，1.8MB，
截图脚本参数错误的产物）。该文件**未被 git 跟踪**，故删除不产生提交记录。
目录复查：无其他首尾带空格或 0 字节的文件。

**截图证据**：保留 `briefs-after-1280.png` 与 `briefs-after-375.png`；
改造前的基线图用完即删（仓库已有大量 before/after 截图，不再增体积）。
